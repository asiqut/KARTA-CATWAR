# SHADOW-SITE

Catwar-inspired map viewer and editor.

## Development

```bash
npm install
npm run dev
```

The first milestone is a lightweight map canvas with locations, a 6×10 transition grid, and editable connectors. Viewer/editor permissions will be added on top of the same data model.